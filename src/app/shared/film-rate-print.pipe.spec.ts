import { FilmRatePrintPipe } from './film-rate-print.pipe';

describe('FilmRatePrintPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmRatePrintPipe();
    expect(pipe).toBeTruthy();
  });
});
