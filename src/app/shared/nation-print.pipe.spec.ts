import { NationPrintPipe } from './nation-print.pipe';

describe('NationPrintPipe', () => {
  it('create an instance', () => {
    const pipe = new NationPrintPipe();
    expect(pipe).toBeTruthy();
  });
});
