import { DDayPrintPipe } from './d-day-print.pipe';

describe('DDayPrintPipe', () => {
  it('create an instance', () => {
    const pipe = new DDayPrintPipe();
    expect(pipe).toBeTruthy();
  });
});
